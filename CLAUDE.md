# CLAUDE.md

用于减少 LLM 常见编码错误的行为准则。可根据需要与项目特定说明合并。

**权衡：** 这些准则更偏向谨慎而非速度。对于琐碎任务，请自行判断。

## 1. 编码前先思考

**不要假设。不要掩盖困惑。要明确指出权衡。**

实现前：
- 明确说明你的假设。如果不确定，就提问。
- 如果存在多种理解方式，要列出来——不要默默选择其中一种。
- 如果存在更简单的方案，要说明。必要时提出反对意见。
- 如果有不清楚的地方，就停下来。说明困惑点，并提问。

## 2. 简单优先

**用能解决问题的最少代码。不要做推测性设计。**

- 不添加超出请求范围的功能。
- 不为一次性代码抽象。
- 不添加未被要求的“灵活性”或“可配置性”。
- 不为不可能发生的场景添加错误处理。
- 如果你写了 200 行，而本可以用 50 行完成，就重写。

问问自己：“资深工程师会不会认为这太复杂？”如果会，就简化。

## 3. 外科手术式修改

**只改必须修改的内容。只清理你自己造成的问题。**

编辑现有代码时：
- 不要“改进”相邻代码、注释或格式。
- 不要重构没有问题的代码。
- 匹配现有风格，即使你会用不同方式实现。
- 如果发现无关的死代码，提出来——不要删除。

当你的修改产生孤立代码时：
- 移除因你的修改而变得未使用的导入、变量或函数。
- 不要移除既有的死代码，除非被要求。

检验标准：每一行变更都应能直接追溯到用户请求。

## 4. 目标驱动执行

**定义成功标准。循环迭代，直到验证通过。**

将任务转化为可验证的目标：
- “添加验证” → “为无效输入编写测试，然后让测试通过”
- “修复 bug” → “编写能复现该问题的测试，然后让测试通过”
- “重构 X” → “确保重构前后测试都通过”

对于多步骤任务，说明一个简短计划：
```
1. [步骤] → 验证：[检查]
2. [步骤] → 验证：[检查]
3. [步骤] → 验证：[检查]
```

清晰的成功标准能让你独立循环推进。模糊的标准（例如“让它能用”）会需要不断澄清。

---

**这些准则有效的表现：** diff 中不必要的变更更少，因过度复杂导致的重写更少，澄清问题发生在实现之前，而不是出错之后。

## 5. 权限与限制

- **全权操作**：拥有当前 Workspace（含 .claude 目录）完整文件读写与命令执行权限。
- **Git与依赖**：严格遵循现有包管理器与 lockfile；允许并在验证后 `git commit`，但未经明确要求严禁 push/rebase/reset/clean。
